
import { useImport } from '@/components/Playground/CanvasMenu';

// Re-export the CanvasMenu from the original location
// This is a temporary solution until we fully migrate the component
export const CanvasMenu = useImport;
